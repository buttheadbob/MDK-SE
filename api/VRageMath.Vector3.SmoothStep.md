← [Index](Api-Index) ← [Vector3](VRageMath.Vector3)

### Summary

```csharp
private public private public sealed struct.Vector3 SmoothStep(private public sealed struct.Vector3 value1, private public sealed struct.Vector3 value2, float amount)
```

Interpolates between two values using a cubic equation.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void SmoothStep(ref private public sealed struct.Vector3 value1, ref private public sealed struct.Vector3 value2, float amount, ref private public sealed struct.Vector3 result)
```

Interpolates between two values using a cubic equation.

### Returns

### Example

### Remarks

