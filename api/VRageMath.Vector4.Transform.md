← [Index](Api-Index) ← [Vector4](VRageMath.Vector4)

### Summary

```csharp
private public private public sealed struct.Vector4 Transform(private public sealed struct.Vector2 position, private public sealed struct.Matrix matrix)
```

Transforms a Vector2 by the given Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(ref private public sealed struct.Vector2 position, ref private public sealed struct.Matrix matrix, ref private public sealed struct.Vector4 result)
```

Transforms a Vector2 by the given Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public private public sealed struct.Vector4 Transform(private public sealed struct.Vector3 position, private public sealed struct.Matrix matrix)
```

Transforms a Vector3 by the given Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(ref private public sealed struct.Vector3 position, ref private public sealed struct.Matrix matrix, ref private public sealed struct.Vector4 result)
```

Transforms a Vector3 by the given Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public private public sealed struct.Vector4 Transform(private public sealed struct.Vector4 vector, private public sealed struct.Matrix matrix)
```

Transforms a Vector4 by the specified Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(ref private public sealed struct.Vector4 vector, ref private public sealed struct.Matrix matrix, ref private public sealed struct.Vector4 result)
```

Transforms a Vector4 by the given Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public private public sealed struct.Vector4 Transform(private public sealed struct.Vector2 value, private public sealed struct.Quaternion rotation)
```

Transforms a Vector2 by a specified Quaternion into a Vector4.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(ref private public sealed struct.Vector2 value, ref private public sealed struct.Quaternion rotation, ref private public sealed struct.Vector4 result)
```

Transforms a Vector2 by a specified Quaternion into a Vector4.

### Returns

### Example

### Remarks

### Summary

```csharp
private public private public sealed struct.Vector4 Transform(private public sealed struct.Vector3 value, private public sealed struct.Quaternion rotation)
```

Transforms a Vector3 by a specified Quaternion into a Vector4.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(ref private public sealed struct.Vector3 value, ref private public sealed struct.Quaternion rotation, ref private public sealed struct.Vector4 result)
```

Transforms a Vector3 by a specified Quaternion into a Vector4.

### Returns

### Example

### Remarks

### Summary

```csharp
private public private public sealed struct.Vector4 Transform(private public sealed struct.Vector4 value, private public sealed struct.Quaternion rotation)
```

Transforms a Vector4 by a specified Quaternion.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(ref private public sealed struct.Vector4 value, ref private public sealed struct.Quaternion rotation, ref private public sealed struct.Vector4 result)
```

Transforms a Vector4 by a specified Quaternion.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(private public sealed class.Vector4[] sourceArray, ref private public sealed struct.Matrix matrix, private public sealed class.Vector4[] destinationArray)
```

Transforms an array of Vector4s by a specified Matrix.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(private public sealed class.Vector4[] sourceArray, int sourceIndex, ref private public sealed struct.Matrix matrix, private public sealed class.Vector4[] destinationArray, int destinationIndex, int length)
```

Transforms a specified range in an array of Vector4s by a specified Matrix into a specified range in a destination array.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(private public sealed class.Vector4[] sourceArray, ref private public sealed struct.Quaternion rotation, private public sealed class.Vector4[] destinationArray)
```

Transforms an array of Vector4s by a specified Quaternion.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Transform(private public sealed class.Vector4[] sourceArray, int sourceIndex, ref private public sealed struct.Quaternion rotation, private public sealed class.Vector4[] destinationArray, int destinationIndex, int length)
```

Transforms a specified range in an array of Vector4s by a specified Quaternion into a specified range in a destination array.

### Returns

### Example

### Remarks

