← [Index](Api-Index) ← [Vector3D](VRageMath.Vector3D)

### Summary

```csharp
private public private public sealed struct.Vector3D Lerp(private public sealed struct.Vector3D value1, private public sealed struct.Vector3D value2, double amount)
```

Performs a linear interpolation between two vectors.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Lerp(ref private public sealed struct.Vector3D value1, ref private public sealed struct.Vector3D value2, double amount, ref private public sealed struct.Vector3D result)
```

Performs a linear interpolation between two vectors.

### Returns

### Example

### Remarks

