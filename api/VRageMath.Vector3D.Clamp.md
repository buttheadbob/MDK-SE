← [Index](Api-Index) ← [Vector3D](VRageMath.Vector3D)

### Summary

```csharp
private public private public sealed struct.Vector3D Clamp(private public sealed struct.Vector3D value1, private public sealed struct.Vector3D min, private public sealed struct.Vector3D max)
```

Restricts a value to be within a specified range.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Clamp(ref private public sealed struct.Vector3D value1, ref private public sealed struct.Vector3D min, ref private public sealed struct.Vector3D max, ref private public sealed struct.Vector3D result)
```

Restricts a value to be within a specified range.

### Returns

### Example

### Remarks

