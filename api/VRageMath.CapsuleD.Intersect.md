← [Index](Api-Index) ← [CapsuleD](VRageMath.CapsuleD)

### Summary

```csharp
private public bool Intersect(private public sealed struct.RayD ray, ref private public sealed struct.Vector3D p1, ref private public sealed struct.Vector3D p2, ref private public sealed struct.Vector3 n1, ref private public sealed struct.Vector3 n2)
```

### Returns

### Example

### Remarks

### Summary

```csharp
private public bool Intersect(private public sealed struct.LineD line, ref private public sealed struct.Vector3D p1, ref private public sealed struct.Vector3D p2, ref private public sealed struct.Vector3 n1, ref private public sealed struct.Vector3 n2)
```

### Returns

### Example

### Remarks

