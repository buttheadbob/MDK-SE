← [Index](Api-Index) ← [Vector3D](VRageMath.Vector3D)

### Summary

```csharp
private public private public sealed struct.Vector3D Hermite(private public sealed struct.Vector3D value1, private public sealed struct.Vector3D tangent1, private public sealed struct.Vector3D value2, private public sealed struct.Vector3D tangent2, double amount)
```

Performs a Hermite spline interpolation.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Hermite(ref private public sealed struct.Vector3D value1, ref private public sealed struct.Vector3D tangent1, ref private public sealed struct.Vector3D value2, ref private public sealed struct.Vector3D tangent2, double amount, ref private public sealed struct.Vector3D result)
```

Performs a Hermite spline interpolation.

### Returns

### Example

### Remarks

