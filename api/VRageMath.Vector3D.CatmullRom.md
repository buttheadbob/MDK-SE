← [Index](Api-Index) ← [Vector3D](VRageMath.Vector3D)

### Summary

```csharp
private public private public sealed struct.Vector3D CatmullRom(private public sealed struct.Vector3D value1, private public sealed struct.Vector3D value2, private public sealed struct.Vector3D value3, private public sealed struct.Vector3D value4, double amount)
```

Performs a Catmull-Rom interpolation using the specified positions.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void CatmullRom(ref private public sealed struct.Vector3D value1, ref private public sealed struct.Vector3D value2, ref private public sealed struct.Vector3D value3, ref private public sealed struct.Vector3D value4, double amount, ref private public sealed struct.Vector3D result)
```

Performs a Catmull-Rom interpolation using the specified positions.

### Returns

### Example

### Remarks

