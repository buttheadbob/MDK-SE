← [Index](Api-Index) ← [Vector2](VRageMath.Vector2)

```csharpvoid Normalize()```##### Summary

Turns the current vector into a unit vector. The result is a vector one unit in length pointing in the same direction as the original vector.

```csharp[Vector2](VRageMath.Vector2) Normalize([Vector2](VRageMath.Vector2) value)```##### Summary

Creates a unit vector from the specified vector. The result is a vector one unit in length pointing in the same direction as the original vector.

```csharpvoid Normalize(ref [Vector2](VRageMath.Vector2) value, ref [Vector2](VRageMath.Vector2) result)```##### Summary

Creates a unit vector from the specified vector, writing the result to a user-specified variable. The result is a vector one unit in length pointing in the same direction as the original vector.

