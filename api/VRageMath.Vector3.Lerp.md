← [Index](Api-Index) ← [Vector3](VRageMath.Vector3)

### Summary

```csharp
private public private public sealed struct.Vector3 Lerp(private public sealed struct.Vector3 value1, private public sealed struct.Vector3 value2, float amount)
```

Performs a linear interpolation between two vectors.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Lerp(ref private public sealed struct.Vector3 value1, ref private public sealed struct.Vector3 value2, float amount, ref private public sealed struct.Vector3 result)
```

Performs a linear interpolation between two vectors.

### Returns

### Example

### Remarks

