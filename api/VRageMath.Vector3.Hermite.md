← [Index](Api-Index) ← [Vector3](VRageMath.Vector3)

### Summary

```csharp
private public private public sealed struct.Vector3 Hermite(private public sealed struct.Vector3 value1, private public sealed struct.Vector3 tangent1, private public sealed struct.Vector3 value2, private public sealed struct.Vector3 tangent2, float amount)
```

Performs a Hermite spline interpolation.

### Returns

### Example

### Remarks

### Summary

```csharp
private public void Hermite(ref private public sealed struct.Vector3 value1, ref private public sealed struct.Vector3 tangent1, ref private public sealed struct.Vector3 value2, ref private public sealed struct.Vector3 tangent2, float amount, ref private public sealed struct.Vector3 result)
```

Performs a Hermite spline interpolation.

### Returns

### Example

### Remarks

